[pytest]
testpaths = tests
addopts = -ra --tb=short
markers =
    integration: marks tests that exercise multiple application components
    ollama: marks tests that require a locally running Ollama service
